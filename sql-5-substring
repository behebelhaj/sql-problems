SELECT name,
       greeting,
       SPLIT_PART(
           SUBSTRING(
               greeting FROM POSITION('#' IN greeting) + 1
           ),
           ' ',
           1
       ) AS user_id
FROM greetings;
